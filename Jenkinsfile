// Noarch (Architecture: all) package — the vendored Node-RED tree is pure-JS
// (built with --omit=optional), so there is no per-arch compilation.
// See https://github.com/wirenboard/jenkins-pipeline-lib
buildDebArchAll()
