# Re-wire (or tear down) the gate + menu when homeui's snippets or share tree
# change — the homeui check must not be a one-shot at our own configure time.
interest-noawait /etc/nginx/snippets
interest-noawait /usr/share/wb-mqtt-homeui
