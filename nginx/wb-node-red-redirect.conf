# Back-compat: the old /node-red path 302-redirects to homeui's gate bounce
# (/open-node-red). Relative target — same origin as homeui, scheme/port-agnostic.
location = /node-red  { return 302 /open-node-red; }
location ^~ /node-red/ { return 302 /open-node-red; }
